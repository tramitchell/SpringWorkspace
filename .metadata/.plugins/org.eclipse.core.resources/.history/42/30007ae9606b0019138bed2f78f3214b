package com.revature.recursion;

public class Factorial {
	// 5! = 5 * 4 * 3 * 2 * 1
	
	public int bangFact(int n)
	{
		int result = 0;
		if(n == 1)
			return 1;
		result = n * bangFact(n - 1);
		return result;
	}
}
